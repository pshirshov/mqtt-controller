[package]
name = "mqtt-controller-wire"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
description = "Shared wire types for mqtt-controller WebSocket API"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
